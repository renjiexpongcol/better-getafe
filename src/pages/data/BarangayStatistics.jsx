import { Link, useParams } from 'react-router-dom'; import { barangays } from '../../data/barangays'; import './publicData.css';
export default function BarangayStatistics() { const { slug } = useParams(); const barangay = barangays.find(item => (item.slug || String(item.name).toLowerCase().replace(/\s+/g, '-')) === slug); return <main className="public-data-page"><section className="data-page-title"><div className="container"><Link className="breadcrumb" to="/data/barangays">Barangay Statistics</Link><p className="data-eyebrow">BARANGAY DATA</p><h1>{barangay?.name || 'Barangay'} Statistics</h1><p>Statistics are shown only when a compatible public dataset is available for this barangay.</p></div></section><section className="container data-section"><div className="data-empty"><h2>No linked observations yet</h2><p>Choose a public dataset and release in the explorer to search available geographic observations. PSGC codes are preserved as strings when returned by the source.</p><Link to="/data/datasets">Explore datasets</Link></div></section></main>; }
