import { Link } from 'react-router-dom'; import { barangays } from '../../data/barangays'; import './publicData.css';
export default function Barangays() { return <main className="public-data-page"><section className="data-page-title"><div className="container"><p className="data-eyebrow">MUNICIPALITY-LEVEL EXPLORER</p><h1>Barangay Statistics</h1><p>Explore available public statistics by barangay. Values appear only when an associated public dataset supplies a compatible observation.</p></div></section><section className="container data-section"><div className="data-empty">Barangay comparison requires a compatible, available public dataset. Browse the dataset catalog to select an indicator and release.<br /><Link to="/data/datasets">Explore datasets</Link></div><div className="barangay-link-grid">{barangays.map(barangay => <Link key={barangay.id || barangay.slug || barangay.name} to={`/data/barangays/${encodeURIComponent(barangay.slug || String(barangay.name).toLowerCase().replace(/\s+/g, '-'))}`}>{barangay.name}<span>Explore available data →</span></Link>)}</div></section></main>; }
